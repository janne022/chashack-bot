node_modules/
dist/
.env
data/
*.log
.DS_Store

# Aspire — generated AppHost SDK + package cache, always regenerated by
# `aspire restore` (which `pnpm dev` runs before starting anything).
.aspire/

# Generated deployment artifacts (`aspire publish`), when we start using it.
aspire-output/
